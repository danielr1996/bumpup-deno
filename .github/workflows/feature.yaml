name: Build
on:
  push:
    branches:
      - '*'         # matches every branch that doesn't contain a '/'
      - '*/*'       # matches every branch containing a single '/'
      - '**'        # matches every branch
      - '!master'   # excludes master
jobs:
  build:
    name: Build
    runs-on: 'ubuntu-latest'
    defaults:
      run:
        working-directory: packages/cli
    strategy:
      fail-fast: false
      matrix:
        deno: ['1.7.5']
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0
      - uses: denolib/setup-deno@v2
        with:
          deno-version: ${{matrix.deno}}
      - run: |
          mkdir dist && deno bundle src/main.ts dist/main.js
          deno run --allow-run --allow-read --allow-write https://raw.githubusercontent.com/danielr1996/bumpup-deno/feature/ci/packages/cli/src/main.ts
          cat version.txt
      - uses: stefanzweifel/git-auto-commit-action@v4
      - name: Push changes
        uses: ad-m/github-push-action@master
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          branch: ${{ github.ref }}

#      - name: Push
#        env:
#          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
#        run: |
#          remote_repo="https://${GITHUB_ACTOR}:${GITHUB_TOKEN}@github.com/danielr1996/bumpup.git"
#          git push "${remote_repo}" ${GITHUB_REF#"refs/heads/"} --follow-tags
#      - name: Publish
#        env:
#          NPM_TOKEN: ${{secrets.NPM_TOKEN}}
#        run: |
#          npx gulp publish